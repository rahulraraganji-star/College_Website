import PageTemplate from "../../Components/PageTemplate";

const Documents = () => (
  <PageTemplate
    slug="admissions-documents"
    title="Documents Required"
    dummyContent={[
      "Applicants must submit the required documents at the time of admission.",
    ]}
  />
);

export default Documents;
