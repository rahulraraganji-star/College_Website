import PageTemplate from "../../Components/PageTemplate";

const Registration = () => (
  <PageTemplate
    slug="alumni-registration"
    title="Alumni Registration"
    dummyContent={[
      "Alumni can register to stay connected with the institution.",
    ]}
  />
);

export default Registration;
