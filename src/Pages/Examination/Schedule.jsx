import PageTemplate from "../../Components/PageTemplate";

const Schedule = () => (
  <PageTemplate
    slug="examination-schedule"
    title="Exam Schedule"
    dummyContent={[
      "The examination schedule provides dates and timings for examinations.",
    ]}
  />
);

export default Schedule;
