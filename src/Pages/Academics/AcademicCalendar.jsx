import PageTemplate from "../../Components/PageTemplate";

const AcademicCalendar = () => (
  <PageTemplate
    slug="academics-calendar"
    title="Academic Calendar"
    dummyContent={[
      "The academic calendar provides important dates and milestones.",
      "It includes examination schedules, holidays, and academic activities.",
    ]}
  />
);

export default AcademicCalendar;
