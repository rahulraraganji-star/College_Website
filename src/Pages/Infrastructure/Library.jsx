import PageTemplate from "../../Components/PageTemplate";

const Library = () => (
  <PageTemplate
    slug="infrastructure-library"
    title="Library"
    dummyContent={[
      "The library houses a wide collection of books, journals, and digital resources.",
    ]}
  />
);

export default Library;
